extends base
include mixins/video

block content
    form(method="GET")
        input(placeholder="Search the video", name="keyword", type="text")
        input(type="submit", value="Search")

    each video in videos
        +video(video)